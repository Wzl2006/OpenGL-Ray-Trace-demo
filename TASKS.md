# 光线追踪 Demo 任务清单

> 状态标记：`[ ]` 未开始 / `[~]` 进行中 / `[x]` 已完成

## 0. 需求锁定（已确认）
- [x] 材质改为单层模型（不做多层材质）
- [x] 球体使用解析求交，不转三角网格
- [x] 前墙不建模（开口）
- [x] Cornell Box 尺寸为 `3 x 3 x 3`
- [x] 顶面中心面光源尺寸为 `1 x 1`，嵌入顶墙
- [x] 色散波长采样 `6` 段；Cauchy 参数采用普通玻璃 `A=1.5, B=0.004`
- [x] GGX 同时用于粗糙反射/粗糙折射；Fresnel 用 Schlick 近似
- [x] 滚轮缩放也触发 accum 重置
- [x] 删除 SPP 参数
- [x] 以功能正确为主，不设性能指标
- [x] 渲染时间按 GPU 时间查询，并在 UI 标注
- [x] 需要详细 README

## 1. 待你提供（路径/环境）
- [x] Qt6 路径（建议给 `CMAKE_PREFIX_PATH`）
- [x] GLM 路径（include 根目录）
- [x] 编译器与生成器（MSVC 版本、Ninja 或 VS 生成器）

## 2. 工程基础搭建
- [x] 初始化 CMake 工程（目标、编译选项、依赖查找）
- [x] 创建 Qt 主窗口 + 左侧固定 `1280x720` 渲染区 + 右侧控制区框架
- [x] 建立 OpenGL 4.3 上下文与 Compute 调度管线
- [x] 准备 SSBO / 纹理 / UBO 等资源管理骨架

## 3. 场景与数据结构
- [x] 内置 Cornell Box 三角形数据（无前墙）
- [x] 定义球体、材质、光源、相机、命中记录等 CPU/GPU 对齐结构
- [x] 建立参数同步机制（UI -> CPU -> GPU）

## 4. BVH 与求交
- [x] CPU 构建三角形 BVH（支持延迟重建）
- [x] 上传 BVH 节点与三角形到 SSBO
- [x] Compute Shader 实现 BVH 遍历 + Moller-Trumbore 三角形求交
- [x] Compute Shader 实现球体解析求交，并与 BVH 结果比较最近命中

## 5. 着色与路径追踪
- [x] 光线生成（像素抖动、光栅化坐标 -> NDC -> 世界空间）
- [x] 迭代式多次弹射（避免递归）
- [x] 漫反射 / GGX 反射 / GGX 折射 / 全反射处理
- [x] Schlick Fresnel + transmission 分支采样（俄轮盘）
- [x] Cauchy 色散（6 波段）
- [x] 面光源多重采样 + 遮挡测试（软阴影）

## 6. 累积与动态重置
- [x] 实现 `accum` 与 `output` 两张 `GL_RGBA32F` 纹理
- [x] 参数/相机变化时清空 accum，并重置累计帧计数
- [x] 静态阶段使用可调 alpha 做指数平均
- [x] 每 250ms 更新统计信息（GPU ms、FPS、累计帧、动态/静态）

## 7. 交互与 UI
- [x] 完成右侧参数面板（按分组，可折叠）
- [x] 实现相机旋转/缩放约束（yaw ±135°, pitch ±20°, distance 2~10）
- [x] 所有参数变化触发正确的“重置或延迟重建”流程

## 8. 文档与验收
- [x] 编写详细 README（构建、运行、参数、架构、关键算法说明）
- [ ] 自测清单（功能点逐项验证）
- [ ] 代码整理与交付说明

## 9. 当前讨论中的实现约定（待最终确认）
- [x] 关于两档 alpha：建议保留
  - 场景变化：清空 accum，首帧等价 `alpha=1`（快速响应）
  - 场景静止：使用 UI 的静态 alpha（默认 `0.05`）持续降噪
